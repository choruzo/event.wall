{
  "name": "event-wall",
  "private": true,
  "type": "module",
  "scripts": {
    "db:init:local": "wrangler d1 execute event-wall --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute event-wall --remote --file=schema.sql",
    "db:migrate:local": "wrangler d1 migrations apply event-wall --local",
    "db:migrate:remote": "wrangler d1 migrations apply event-wall --remote",
    "db:seed:cafe:local": "wrangler d1 execute event-wall --local --file=examples/cafe-helmcode.sql",
    "db:seed:cafe:remote": "wrangler d1 execute event-wall --remote --file=examples/cafe-helmcode.sql",
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "check": "node --check public/app.js && node --check src/lib.js && node --check functions/api/admin/events/index.js && node --check functions/api/admin/events/[slug].js && node --check functions/api/events/index.js && node --check functions/api/events/[slug]/index.js && node --check functions/api/events/[slug]/participants/index.js && node --check functions/api/events/[slug]/participants/[id].js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
